---
layout: default
title: "task2(5)头等舱 - F12 网络面板查看响应头获取 Flag"
---

# task2(5)头等舱：F12 网络面板查看响应头获取 Flag

## 一、题目信息
平台：CTF靶场  
类型：web

## 二、解题思路
这类题目考察的是选手是否知道 **HTTP 响应头（Response Headers）** 中可能隐藏敏感信息。  
网页在浏览器中渲染后展示的是页面内容，但服务器返回的 HTTP 响应头里，开发者有时会不小心把 Flag 写在自定义头字段中。我们需要通过浏览器的开发者工具，进入 **Network（网络）** 面板，查看请求的响应头，从中找到 Flag。

## 三、解题步骤
1. 打开目标网页。
2. 按下 `F12` 打开开发者工具。
3. 切换到 **Network（网络）** 标签页。
4. 刷新页面（`F5`），让 Network 面板捕获请求。
5. 在请求列表中找到主文档请求（通常是第一个，Name 列显示为域名或 `/`）。
6. 点击该请求，在右侧面板中切换到 **Headers（标头）** 标签页。
7. 向下滚动到 **Response Headers（响应标头）** 区域。
8. 查找自定义字段（如 `Flag`、`X-Flag`、`X-Forwarded-For` 等），找到形如 `flag{...}` 的字符串，即为 Flag。
