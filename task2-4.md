---
layout: default
title: "task2(4) - ESC 停止刷新 + Ctrl+U 获取 Flag"
---

# task2(4)：ESC 停止刷新 + Ctrl+U 获取 Flag

## 一、题目信息
平台：CTF靶场  
类型：web

## 二、解题思路
这类题目是 Web 方向的基础题，考察的是面对**页面快速自动刷新**时，如何停止刷新并查看源代码。  
页面通过 JavaScript 定时器不断刷新，导致来不及看清内容。我们需要在**正确图片出现的那一刻按下 ESC** 停止刷新，也可以通过控制台输入代码清除控制器，再通过 `Ctrl+U` 查看源代码，从中找到 Flag。

## 三、解题步骤
1. 打开目标网页，观察页面在快速自动刷新。
2. 盯住页面内容，当**正确图片显示出来**时，立刻按下 `ESC` 键，停止页面加载和 JS 定时器。
3. 此时页面静止，按 `Ctrl+U` 查看网页源代码。
4. 在源代码中搜索关键词 `flag`，找到形如 `flag{...}` 的字符串，即为 Flag。
